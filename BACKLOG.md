# Windex — Backlog

- **Verification rule: visual inspection required for user-facing output.** For any change affecting user-facing output (emails, UI strings, push notifications, generated reports), verification must include visual inspection of the actually-rendered output, not just database state or API response. Auth-log routing analysis, "PATCH returned 200," and database state checks verify *behavior*, not *rendering*. Origin: 2026-05-14 OTP-cutover session. The Phase 1 template-content PATCH appeared to succeed by every database-state and API-routing signal — PATCH returned 200 with our values echoed back, auth logs showed correct action routing through `user_confirmation_requested`, the diagnostic Edge Function showed clean `email_confirmed_at` / `last_sign_in_at` / `auth.sessions` state — but the rendered email body was the Supabase default magic-link template with `{{ .ConfirmationURL }}`. Caught only when Buzz did the visual `+alias` test against a real email client. Lesson: end-to-end-from-user's-eye is the load-bearing check; every other signal is supplementary. Build at least one visual checkpoint into every fix that affects rendered output, BEFORE declaring fix-complete.

- **Refine OTP email copy for email-change and reauthentication flows.** All five overridden GoTrue email templates (`mailer_templates_{magic_link,confirmation,invite,recovery,email_change,reauthentication}_content`) share a single body at `windex-api/supabase/templates/otp.html` that reads "Enter this code to sign in." That phrasing is accurate for sign-in/invite/confirmation/recovery flows but slightly imprecise for the two dormant flows: email-change ("…to confirm your new email") and reauthentication ("…to confirm your action"). Both flows have no user-facing UI today — the templates exist solely as defense-in-depth against the scanner/prefetcher attack on `{{ .ConfirmationURL }}` (see the 2026-05-14 cutover). If/when either flow gets a real UI, branch the template content (introduce `otp-email-change.html`, `otp-reauthentication.html`) and PATCH the corresponding `mailer_templates_*_content` fields with the new bodies. Keep `otp.html` as the shared body for the sign-in family.

- ~~**Tighten Players page Status badge derivation.**~~ **Done 2026-05-17 (migration 028, deployed to prod).** `get_players_auth_status()` now sets `has_signed_in = (email_confirmed_at IS NOT NULL OR last_sign_in_at IS NOT NULL) AND EXISTS (SELECT 1 FROM public.activity_events WHERE player_id = p.id)`. Only the `has_signed_in` column expression changed; arg list, return shape, plpgsql/STABLE/SECURITY DEFINER, `SET search_path`, the `am_i_super_admin()` gate, COMMENT, and GRANT are all preserved, so the UI's `deriveStatus()` is untouched. **Post-deploy production verification (replicated the OLD vs NEW truth conditions via privileged join — the SECURITY DEFINER `am_i_super_admin()` gate returns 0 rows to the MCP `postgres` connection by design, so the RPC can't be called directly without a super-admin JWT):** 26 players total. **10 rows flip green→yellow** (the visible tightening), splitting into two clusters: (a) **3 genuine phantom redemptions** — `X`, `Dietz`, `Dr. Chris` — each its own auth user, distinct `invited_at` (~2026-03-27) / `email_confirmed_at` / early-May `last_sign_in_at`, zero `activity_events`; exactly the scanner/prefetcher failure mode this item targeted, and NOT the 2026-05-14 BLSmooth/Dagger/Frank set (that cleanup only covered YC Windex's three — these 3 are separate historical survivors). (b) **7 rows sharing one auth user** `c49d7e41-…` / `dev@lateaddgolf.com` — `Buzz YC Weds`, `CC`, `DD`, `Frenchie`, `Getty`, `Momo`, `Rage` — identical `email_confirmed_at`/`last_sign_in_at`/`created_at` to the microsecond because they all FK the shared dev/seed auth user (see the "Migrate `dev@lateaddgolf.com`" backlog item); correctly turned yellow, but the green here was a side-effect of the shared dev row, not 7 independent phantoms. **Divergences from the pre-task expectation:** `BLSmooth` is NOT a Case-2 yellow — post-2026-05-14 re-invite it genuinely signed in and has activity, so it's correctly green under both old and new logic (only `Dagger` and `Frank` of that trio are yellow, alongside `Adam`/`Neal`/`Rosie`, all auth-row-but-never-signed-in). Case-3 (grey/Created, `user_id IS NULL`) has **zero** production rows — every one of the 26 players has an auth row — so that path is logically unchanged but couldn't be exercised on live data. Note `Buzz YC Weds` (now yellow) is a dev-user phantom, distinct from the real `Buzz` (green) — expected, not a regression.

- **Clean up three additional historical phantom-confirmed players (X, Dietz, Dr. Chris).** Each is its own independent scanner/prefetcher phantom — a distinct `auth.users` row, ~2026-03-27 `invited_at`, `email_confirmed_at` + early-May `last_sign_in_at` populated, **zero** `public.activity_events`. Surfaced 2026-05-17 by migration 028's post-deploy verification (they now correctly render `invited` / yellow on the Players page). They predate the 2026-05-14 YC Windex investigation and were **not** cleaned up in that session's Phase 2 because they weren't in YC Windex. Their auth rows can be safely cleaned the same way BLSmooth/Dagger/Frank were — per player: `UPDATE public.players SET user_id = NULL` → `DELETE FROM auth.users WHERE id = <uuid>` (cascades `auth.sessions` / `auth.refresh_tokens` / `auth.identities`) → re-invite via the new OTP-code `send-invite` flow (the migration-020 `link_player_on_auth_signup` trigger re-links `players.user_id` on the fresh auth row). **Only do this for players who actually intend to use the app.** Otherwise leave them yellow — they heal naturally the moment they genuinely sign in (auth signal + first `activity_events` row → green under migration 028's tightened derivation). Distinct from the seven-player `dev@lateaddgolf.com` shared-auth cluster (`Buzz YC Weds`, `CC`, `DD`, `Frenchie`, `Getty`, `Momo`, `Rage`), which migration 028 also correctly turns yellow but whose root cause stays scoped to the separate "Migrate `dev@lateaddgolf.com` auth user" item — do not touch that cluster as part of this cleanup.

- ~~**Standings shows retired players in every season, even ones they never played.**~~ **Done 2026-05-18 (migration 030, deployed to prod; PR `fix/standings-filter-retired`, not yet merged).** Side effect of retirement Phase 1 (029): `season_standings` anchors on `group_members × seasons`, so a retired player appeared in EVERY season of their group (Frenchie: 16+ Windex Cup rows, only 1 with an actual round). Buzz's rule — retired players (`players.retired_at IS NOT NULL`) appear in standings ONLY where `rounds_played > 0`; active players (`retired_at IS NULL`) appear in all seasons exactly as before. Migration 030 `CREATE OR REPLACE`s the view: adds `LEFT JOIN players p ON p.id = gm.player_id`, appends `p.retired_at` to `GROUP BY` (functionally constant per `player_id` → provably aggregation-neutral), and `HAVING p.retired_at IS NULL OR COUNT(DISTINCT lr.id) > 0`. LEFT JOIN so orphan memberships / active players are never dropped. All 8 columns / aggregations / joins / `WHERE gm.is_active=1` preserved verbatim vs migration 019; view has no ORDER BY (callers order themselves). **Post-deploy proof — old-019-logic vs new-030 on identical CURRENT data (not the day-old baseline): `rows_removed = 16` exactly equals retired players' zero-round rows (`16`), `active_rows_lost = 0`, Buzz old==new==35, Frenchie 17→1 with the kept row byte-identical (`AUE4-xpZRyeLb8zGE6vRcA`: rounds_played=1, total_points=-9, wins=0, losses=1, ties=0).** Absolute counts differ from the pre-task baseline (expected total 265 / drop 15; actual 285 / drop 16) **purely because the `windex-season-rollover-daily` cron fired `2026-05-18 06:00 UTC` and created 3 new seasons overnight** (Windex Cup `4kKh0sk1QpSvjPE6p8VNKA`, Adam TBD `9minki2kSgwAkL3Mi49A`, YC Windex `a-f-NDMUNTtGA6JxDT8S9CQ`) — the documented over-projection cron, NOT a regression; the old-vs-new diff controls for it. No windex-expo change needed: the only count indicator (`getActiveMemberCount`) is independent of `season_standings` and already retirement-aware; the all-time standings aggregate treats zero rows as arithmetic no-ops. PR open, not merged — Buzz visual-checks prod windex-expo standings first.

- ~~**GSD hooks fail on Windows when Claude Code routes commands through bash.**~~ **Locally resolved 2026-05-17 (fix + in-session verification appended at the end of this entry).** Every GSD hook in `~/.claude/settings.json` is registered with PowerShell call-operator syntax (`& "C:/Program Files/nodejs/node.exe" "<hook>.js"` or `& bash "<hook>.sh"`). Claude Code invokes hook commands via `/usr/bin/bash -c`, where leading `&` is the background-process operator, not the PowerShell call operator. Result: every hook invocation fails with `syntax error near unexpected token '&'` before the hook script can run. PreToolUse variants block their tool calls (PreToolUse:Write|Edit hooks `gsd-prompt-guard.js`, `gsd-read-guard.js`, `gsd-workflow-guard.js`). PostToolUse variants fail silently (`gsd-context-monitor.js`, `gsd-read-injection-scanner.js`, `gsd-phase-boundary.sh`). The bug applies to ALL GSD hooks registered: `SessionStart` (`gsd-check-update.js`, `gsd-session-state.sh`), all `PreToolUse` matchers (`Write|Edit` × 3, `Bash` × 1), all `PostToolUse` matchers (`Bash|Edit|Write|MultiEdit|Agent|Task`, `Read`, `Write|Edit`), and the `statusLine` command (`gsd-statusline.js`). Workaround applied 2026-05-14: 5 most-impactful hooks disabled via empty `"hooks": []` arrays in `settings.json`; original committed to `settings.json.bak`; restored at session end so future sessions start with the broken-but-original config (avoids silently losing the GSD machinery without realizing it). **RESOLUTION 2026-05-17:** dropped the leading `& ` from all 10 hook commands in `~/.claude/settings.json` (7 `node`-path hooks + 3 `bash "…sh"` hooks); JSON re-validated; original backed up to `~/.claude/settings.json.bak.broken-hooks` (distinct from the 2026-05-14 `.bak`). Verified in-session: Write, Edit, Bash, and Read tools all execute cleanly with no hook errors and no blocks — Claude Code re-reads `settings.json` mid-session, so no restart was required. Post-fix the hooks are benign: `gsd-read-guard.js` no-ops under Claude Code (session_id detection); `gsd-prompt-guard.js`, `gsd-workflow-guard.js`, `gsd-read-injection-scanner.js`, `gsd-context-monitor.js` are advisory-only (emit `additionalContext`, never `decision:block`); `gsd-validate-commit.sh`, `gsd-phase-boundary.sh`, `gsd-session-state.sh` are opt-in no-ops because Windex has no `.planning/config.json` (so the commit-message validator does NOT enforce Conventional Commits here). Upstream GSD report tracked as a separate item below.

- ~~**Player retirement — Phase 1 (visibility only).**~~ **Implemented 2026-05-17 (migration 029 + windex-admin/windex-expo code; PR `feat/player-retirement` open). Migration push to prod and the Frenchie data remediation are pending Buzz approval — see status line.** A "resigned" player (origin: Frenchie) must vanish from operational surfaces but keep all historical scored rounds in standings. Investigation found the existing `is_active` mechanism is unusable for this: `season_standings` (migrations 018/019) anchors on `group_members` with `WHERE gm.is_active=1`, so `gm.is_active=0` *erases* a player from all historical standings (already silently true for Frenchie and Getty — each had 1 real Windex Cup round hidden). **Design (approved):** new orthogonal `players.retired_at TIMESTAMPTZ NULL` (migration 029). Single-axis model — retirement is expressed *only* by `retired_at`; retired players keep `is_active=1` on BOTH `players` and `group_members` so every history/standings surface (season_standings, get-points-matrix, cup-champion name resolution, activity_events) keeps showing them. Only operational reads filter `retired_at IS NULL`: admin `listPlayersWithMembership` (default tab) + `listAllPlayers` (Create Group picker), expo `AddRoundModal` score-entry picker, expo `getActiveMemberCount`. windex-admin Players page gains an Active/Retired tab with Retire/Unretire (PATCH `players.retired_at`), reusing the existing `STATUS_PILL`/ConfirmModal patterns. No RLS migration needed (`players_update` already allows super-admin PATCH; `players_select`/`group_members_select` are `USING (true)`). No Edge Function changes — historical paths intentionally untouched. **Status:** code complete + type-checked clean (windex-admin `tsc -b`, windex-expo `tsc --noEmit`); migration 029 SQL written but **not pushed** (awaiting Buzz review per project rule); Frenchie remediation SQL drafted but **not run** (awaiting Buzz approval); PR open, not merged. Phase 1 deliberately does NOT revoke a retired player's ability to act if they have their own auth row — see Phase 2 item below.

- **Phase 2: harden `am_i_group_member()` / `am_i_group_admin()` to exclude retired players.** Deferred from the 2026-05-17 player-retirement Phase 1 (scope was visibility only). Those two SECURITY DEFINER helpers (migration 014) resolve membership via `player_id IN (SELECT id FROM players WHERE user_id = auth.uid())` with **no `is_active` or `retired_at` check**, so a retired player who has their own real `auth.users` row could still pass member/admin-gated RLS (create rounds/scores, etc.). Moot today — Frenchie/Getty share the `dev@lateaddgolf.com` auth row and can't log in as themselves — but a future retired player with a genuine auth account would not be access-revoked by retirement. Fix when needed: add `AND retired_at IS NULL` to the player sub-select in both helpers (new migration). Treat as a security-hardening follow-up, not a visibility bug; verify no legitimate flow depends on a retired player retaining write access before shipping.

- **Historical `is_active=0` cleanup: Getty (Windex Cup).** Surfaced by the 2026-05-17 retirement Step-0 scan. Exactly two players in the whole DB have any `is_active=0` condition and both have a real scored round hidden from `season_standings` by the `WHERE gm.is_active=1` view filter: **Frenchie** (`y4gWEVBrS5uEdhL97TIxTg`) — remediated this session as the feature's test case — and **Getty** (`rKhO7RujSGeWGOsxLOiolg`, group `4kKh0sk1QpSvjPE6p8VNKA` Windex Cup, `p.is_active=0`/`gm.is_active=0`, 1 hidden round). Buzz chose to remediate **Frenchie only** this session. Getty's 1 Windex Cup round remains incorrectly absent from standings until cleaned. Remediation is the same single-axis fix: `UPDATE players SET is_active=1 WHERE id='rKhO7RujSGeWGOsxLOiolg'`; `UPDATE group_members SET is_active=1 WHERE player_id='rKhO7RujSGeWGOsxLOiolg'`; then optionally `UPDATE players SET retired_at=now()` if Getty should also be marked retired (decide intent first — restoring is_active alone un-hides the round; setting retired_at additionally hides Getty from operational lists). No other broken players exist (scan was exhaustive).

- **Report the GSD hook `&`-prefix Windows bug upstream.** Anyone installing GSD (`npx get-shit-done-cc`) on Windows hits this: the installer writes every hook command with a leading `& ` (PowerShell call-operator syntax), but Claude Code runs hook commands through `/usr/bin/bash -c`, where a leading `&` is the bash background-process operator → every hook dies with `syntax error near unexpected token '&'` before its script runs. Fix on the GSD side: emit hook commands WITHOUT the `& ` prefix — both `"C:/…/node.exe" "…hook.js"` and `bash "…hook.sh"` run correctly under `bash -c` — or detect the Claude-Code/bash runtime and stop emitting the PowerShell call operator. File via the GSD issue tracker with the before/after diff and a Windows repro. **Test-harness caveat for whoever reproduces this:** invoking the `bash "…hook.sh"` hooks from PowerShell (or any non-Git-Bash shell) can resolve bare `bash` to `C:\Windows\System32\bash.exe` (the WSL stub) and fail with `execvpe(/bin/bash) failed: No such file or directory`. That is a PATH artifact of the test harness, NOT a GSD bug — Claude Code's own hook environment resolves `bash` to Git Bash correctly (verified 2026-05-17: the `PreToolUse:Bash` `gsd-validate-commit.sh` hook ran fine and did not block the Bash tool). Don't "fix" GSD for the WSL-stub symptom.

- **Standalone Players admin screen in windex-admin.** Today `windex-admin/src/pages/Players.tsx` is group-scoped — you have to pick a group from the dropdown before any player rows show up. Players who have been created via the Add Player flow but have not been assigned to any group are invisible. Same problem for any future "inline-created player in Create Group" who fails the group submission step: the player record persists but the admin can't find them again without knowing the player_id. Build a `/players` standalone view that lists every player in `public.players` regardless of group membership, with the same Edit + Send Invite affordances. Likely also wants a filter for "unlinked" (`user_id IS NULL`) so admins can find pending invites quickly. Surfaced 2026-05-13 while shipping send-invite (which only reaches players via the group-scoped page).

- **Drop `groups.admin_player_id` column.** Legacy Glide v1 single-admin pointer; zero runtime readers verified 2026-05-13 (windex-admin, windex-expo, all Edge Functions, all RLS policies, and SQL helpers — `am_i_group_admin()` uses `group_members`, not this column). All writes come from the retired Glide import scripts (`sync-glide-structure.mjs`, `generate-seed-bundle.mjs`) and pass-through in `adminSeed.ts`. Before dropping: audit any future RLS or query work to confirm no implicit dependency, then `ALTER TABLE groups DROP COLUMN admin_player_id`. Also strip the column writes from `sync-glide-structure.mjs`, `generate-seed-bundle.mjs`, and `adminSeed.ts`. Documented "Not currently used in RLS or code" in `Windex_Permissions_Spec.md:28`.

- **Storage GC pass: orphan `catta-rage.jpg` in `group-images` bucket.** Verified 2026-05-13 — the bucket holds three objects (`windex-cup.png`, `yc-windex.png`, `catta-rage.jpg`) but only the first two are referenced by `groups.logo_url`. No "Catta Rage" group exists in `public.groups`. Scope of a future GC pass: query `storage.objects` against `groups.logo_url` to find any object whose public URL isn't referenced; surface the list; one-shot manual deletion via `storage.from('group-images').remove([...])`. Not urgent — `catta-rage.jpg` is 526 KB, no cost pressure.

- **Vercel password protection on `admin.windexgolf.com` (defense-in-depth, optional).** Today the admin app at `admin.windexgolf.com` is publicly reachable; security is enforced by Supabase RLS (every admin-only operation is gated by `am_i_super_admin()` or per-table policies). That's correct and sufficient — the URL itself doesn't grant access to anything. Vercel offers "Password Protection" as an add-on (Pro plan and up) that puts a password wall in front of the deploy, before users even see the login page. Reasons we'd want it: (a) reduce the surface area for opportunistic discovery / bot scanning; (b) avoid load on the Supabase auth endpoint from drive-by traffic; (c) make the audit log cleaner by not capturing failed-OTP attempts from randos who Google'd "site:vercel.app admin". One-checkbox setting in the Vercel project dashboard once enabled. Cost is a single shared password; not worth it today (low traffic, no signals of abuse) but cheap to flip on if anything shifts.

- ~~**Cup Champion per season — phone app display + admin manual input.**~~ **Done 2026-05-11.** Migration 025 added `cup_champion_player_id` (TEXT, FK → `players.id` ON DELETE SET NULL) and `cup_champion_notes` (TEXT) to the `seasons` table, plus a backfill of six historical Windex Cup champions (2020-2025); YC Windex and all current/future seasons remain NULL. New admin tab at `/cup-champions` (super-admin only) renders a per-group table with Set/Edit Champion modal — champions can be set on future seasons too (not blocked until `end_date` passes), so admins can record a cup the day after the playoff finishes even if the season is still "open" on paper. Phone app Previous Seasons cards reworked into an inline layout: year + "Cup Champion: <full_name>" on the same line, "Points Winner: <full_name>  <N>" indented below, no badges, no date-range hint. Names render `players.full_name` on both rows (admin's player-picker dropdown intentionally still uses `display_name` since admins recognize nicknames). Edge Function `seasons` bumped to v4 to pass `cup_champion_player_id` and `cup_champion_notes` through to clients. Locked decisions: standalone tab (not folded into GroupDetail), no current-season Cup Champion shown on the phone app at all (admin can still set it early), no audit-log entry for championship assignments (manual + low-frequency, not worth the wiring).

- **Season-rollover over-projects future years (low priority, cosmetic):** `ensure_next_season_for_group()` (migration 021) is supposed to keep exactly one future season ahead of each group, but the idempotency check is "does a season starting on `<recent_end + 1 day>` already exist?" — and `recent_end` is recomputed each daily run from the *most recent* end_date. So once 2027 exists, the next day's run sees 2027's end_date as the new "recent" and creates 2028; the day after, 2029; and so on. Observed 2026-05-11: both Windex Cup and YC Windex have seasons 2027 / 2028 / 2029 already created instead of the intended single year ahead. Not user-visible — `windex-expo/lib/api.ts listSeasons` filters out `start_date > today`, so future rows never reach the picker. Fix: change the function to bail out once any season with `start_date > today` exists for the group, so the rollover only fires once we're within a calendar year of needing the next one. Until fixed, the daily cron will continue to add one more future row per day until manually trimmed.

- **Leap-year drift in season-rollover dates (low priority, cosmetic):** `ensure_next_season_for_group()` (migration 021) computes `next_end = next_start + INTERVAL '1 year' - INTERVAL '1 day'`. When `next_start` is March 1 of a year following a leap year (or any date crossing a Feb-29 boundary), Postgres `+ INTERVAL '1 year'` truncates to the last valid day of February, which makes the resulting `end_date` drift back one day. For golf league seasons this is invisible — Windex Cup's Dec-Nov and YC Windex's Sept-Aug never cross Feb-29 in their start_date. Flagged in case Windex ever picks up a group whose start_date IS in March, in which case we'd want to switch to month-based arithmetic instead.

- ~~**Adam TBD seeded with only future-dated seasons (no current season, broke the Rounds view).**~~ **Resolved 2026-05-17 (hybrid A1 + B fix).** `create-group` seeds no season; the seed comes from the windex-admin `CreateSeasonModal`, whose zero-season default used `new Date().getFullYear()` + `start = year-ssm-01`. For "Adam TBD" (created 2026-05-13, `season_start_month=9`) that produced a first season starting 2026-09-01, ~3.5 months in the FUTURE, so `windex-expo` `listSeasons()` (`start_date <= today`) returned [] and `selectedSeason` was null (the second, separate cause behind the 2026-05-15 chicken-and-egg report; the header bug itself shipped in PR #8). The daily rollover cron then chained 2028..2031 onto the bad seed (the separate cosmetic over-projection item above, unchanged). **A1 data fix:** inserted the real prior-cycle season `sn_9minki2kSgwAkL3Mi49A_2026` (2025-09-01 to 2026-08-31), mirroring sister group YC Windex (same `ssm=9`); contiguous with the existing `_2027` (starts 2026-09-01); verified current-as-of-today; the 9 historical star rows untouched. **B code fix:** `windex-admin/src/components/CreateSeasonModal.tsx` now computes a seed year that CONTAINS today for zero-season groups (`todayMonth >= ssm ? todayYear : todayYear-1`); groups with existing seasons unchanged (still extend the chain via `defaultYear`). `tsc --noEmit` clean. Branch `fix/admin-season-seed-defaults`, PR open (awaiting Buzz review + Vercel preview of the season modal).

- **Move first-season seeding server-side into `create-group`.** A new group still has NO season until a super admin manually opens the windex-admin Create Season modal; forget, and the group sits in the "Adam TBD" broken state until noticed. The 2026-05-17 B fix corrects the modal's default dates but still depends on a human running it. Consider seeding the current-window season inside the `create-group` Edge Function right after the groups insert (compute from `season_start_month` + today with the same `todayMonth >= ssm ? todayYear : todayYear-1` rule, id `sn_<group_id>_<endYear>`), so every new group is immediately usable. Deferred from the 2026-05-17 hybrid fix (scope was modal-only). Server-side change: needs an edge-function deploy and rollback handling consistent with create-group's existing atomicity.

- ~~**Activity viewer in windex-admin — covers both login_events and user_events.**~~ **Done 2026-05-11.** "App Activity" admin tab at `/activity` (list) and `/activity/:player_id` (detail) consumes the `public.activity_events` view (migration 024) plus three RPC helpers (`get_players_with_last_activity`, `get_player_activity_timeline`, `get_player_activity_summary`). Token refresh is excluded at the view level. Super-admin gated via `isCurrentUserSuperAdmin()` + RLS on the underlying tables. Naive "views per group" queries on user_events should filter `event_type = 'view_leaderboard'` explicitly, because a group switch fires `group_switch` + a view_* event per currently-mounted tab — documented in `windex-expo/lib/userEvents.ts` header.

- ~~**Manual linking after each Glide import:**~~ **Obsolete — Glide imports are retired** (one-time launch migration, complete). New player onboarding is exclusively via the unified Add Player flow on `windex-admin/Players.tsx` (`+ Add Player` button → `invite-player` Edge Function), which also auto-links the auth user via the `link_player_on_auth_signup` trigger added in migration 020 (2026-05-09). The `windex-api/scripts/sync-glide-members.mjs` and friends are kept for historical reference only.

- ~~**Pre-existing Players.tsx edit bug (low priority):**~~ **Done 2026-05-09.** `updatePlayer()` now PATCHes by `id` only and relies on RLS (`players_update`: super admin OR owning user) for permissions. The `userId` parameter was removed from the function signature; the sole call site in `Players.tsx#handleSave` was updated to match.

- **Future access-control review:** Evaluate whether round-level actions should authorize by `league_rounds.user_id` or by group-based permissions derived from membership/role in the associated group.

- **Scorekeeper integration:** When Scorekeeper is ready, wire up `POST /ingest-event-results` as the submission target after a round is scored. Requirements:
  - Player ID mapping between Scorekeeper and Windex (use `player_mappings` table with `source_app = "scorekeeper"`)
  - Use `source_app = "scorekeeper"` and `external_event_id` for idempotency (prevents duplicate rounds)
  - Send `game_points` per player — Windex computes the head-to-head differential (`score_value = N × game_points - round_total`) server-side
  - Reference `shared-golf-types` for shared player/round TypeScript types
  - Scorekeeper does NOT need to compute standings or differentials — Windex handles all aggregation
  - Test with the adapter pattern in `windex-api/adapters/` and `docs/SOURCE_ADAPTERS.md`

- ~~**Round edit/delete permission enforcement:**~~ **Done.** Edit/delete buttons on Round Detail are now hidden for members. `GroupContext` resolves `isSuperAdmin` and `isGroupAdmin(groupId)` on login via RPC. Backend RLS enforces at DB level as backup.

- **Migrate `dev@lateaddgolf.com` auth user to `dev@windexgolf.com`:** The Supabase Auth user with email `dev@lateaddgolf.com` (UUID `c49d7e41-...`) owns all bulk-created players and is hardcoded as the admin login pre-fill. Migration steps:
  1. Create new Supabase auth user with email `dev@windexgolf.com` (via dashboard or `admin.createUser`)
  2. Migrate data tied to the old user's UUID — primarily `players.user_id` rows (likely the bulk-created roster); audit other tables with `user_id` FKs (`groups`, `sections`, `league_rounds`, etc.) for any rows owned by the old UUID
  3. Update the 5 code/doc references inventoried in the 2026-05-05 cutover Phase 5 Group E:
     - `Windex_Permissions_Spec.md:22`
     - `windex-admin/src/pages/Login.tsx:20`
     - `windex-admin/README.md:33`
     - `windex-admin/README.md:40`
     - `windex-admin/README.md:59`
  4. Delete the old auth user once data migration is verified
  5. Coordinate with `lateaddgolf.com` email DNS/MX cleanup if/when that happens (currently MX records are unchanged so the old email still receives mail)

- **Rounds tab hid the season heading + Add Round button whenever the rounds list was empty (chicken-and-egg).** Root cause of the 2026-05-15 "missing 2026 season label + Add Round button" report. In `windex-expo/app/(tabs)/rounds.tsx` both the prominent `{year} Season` heading and the Add Round button live inside `listHeader`, which was the FlatList `ListHeaderComponent` gated by `events.length > 0`; the events query is season-filtered. Any group whose season-filtered rounds list was empty rendered neither element (only "No rounds found."). Concretely: DB group "YC Windex" (id a-f-NDMUNTtGA6JxDT8S9CQ) has a valid current season but 0 league_rounds, so its Add Round button was unreachable - no way to add the FIRST round. DB group "Adam TBD" (id 9minki2kSgwAkL3Mi49A) is hit by this AND a second independent cause - all 3 of its seasons are future-dated (earliest 2026-09-01; created 2026-05-13, season_start_month = 9), so `listSeasons()` start_date <= today filter (`windex-expo/lib/api.ts:277`) returns [] and selectedSeason is null. The flagship "Windex Cup" (id 4kKh0sk1QpSvjPE6p8VNKA, 467 rounds) worked only because it had BOTH a current season and rounds. NOT an RLS/auth issue. **Decision 2026-05-15: fixed via option A** - `ListHeaderComponent` now always renders `listHeader`; the "No rounds found" empty state moved into the FlatList `ListEmptyComponent` (guarded by `!loadingEvents && selectedGroup && !error`) so the season heading + Add Round button sit above the empty message and the message no longer flashes during load. Implemented on branch `feat/fix-empty-rounds-header` (PR open; awaiting Buzz prod verification on YC Windex per the visual-inspection rule above). The Adam-TBD-class future-dated-season seeding gap is a SEPARATE follow-up - see the season-rollover seed item below; not addressed here.

- **`windex-expo/README.md` still documents the removed password fallback.** Lines 20 and 26 describe an "Email/password fallback … behind a 'Sign in with password instead' toggle" and list `signInWithPassword` among AuthContext's exposed methods. The player-facing toggle/form was removed 2026-05-18 (`fix/remove-password-link-from-player-login`); `signInWithPassword` still exists in `contexts/AuthContext.tsx` but is no longer consumed anywhere in the app. README was outside that task's authorized edit set so it was intentionally left stale rather than silently edited. Doc-only follow-up: reword line 20 (the password fallback is no longer a player affordance) and decide whether line 26 should note `signInWithPassword` is now an unused/admin-parity export or drop the mention. No code impact.

- **Admin UI: add existing player to an additional group.** Currently the windex-admin Add Player flow only creates NEW players (inserts a `players` row + sends an invite). When an existing player needs to join an additional group, there's no UI path - it requires a manual SQL INSERT into `group_members` (done 2026-05-15 to add Buzz to "Adam TBD" so the season-gating bug could be reproduced as a member). Add an "Add Existing Player" affordance on the Group Detail page (`windex-admin/src/pages/GroupDetail.tsx`) that opens a player picker filtered to players not currently in this group and inserts the `group_members` row (id pattern `gm_<group_id>_<player_id>`, `role` default `'member'`, RLS already allows super-admin / group-admin). No auth invite needed since the player already exists. Surfaced 2026-05-15.
