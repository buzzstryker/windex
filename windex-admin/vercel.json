{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "ignoreCommand": "git fetch --unshallow --quiet 2>/dev/null || true; git diff --quiet \"${VERCEL_GIT_PREVIOUS_SHA:-HEAD^}\" HEAD -- .",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
