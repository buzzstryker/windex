{
  "name": "windex-repo",
  "private": true,
  "description": "Monorepo root — mobile app lives in mobile/windex",
  "scripts": {
    "mobile": "npm run start --prefix mobile/windex",
    "start": "npm run start --prefix mobile/windex",
    "start:tunnel": "npm run start:tunnel --prefix mobile/windex",
    "start:lan": "npm run start:lan --prefix mobile/windex"
  }
}
